{
  "default": {
    "paths": ["tests/features/**/*.feature"],
    "require": ["tests/e2e/steps/**/*.ts"],
    "requireModule": ["ts-node/register"],
    "format": ["progress", "html:playwright-report/cucumber-report.html"]
  }
}
